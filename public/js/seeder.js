// seeder.js
console.log("seeder.js connected");
const allImages = {};

function getImages() {
    let url = window.location.href;
    console.log(url);
    // allImages = window.location.href("/ops/seed");
    // allImages.forEach(image => {
    //     console.log(image);
    // });
}